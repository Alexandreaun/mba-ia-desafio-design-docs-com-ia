# Índice de ADRs Potenciais

## Progresso da Análise

### Módulos Analisados
- **WEBHOOKS**: Módulo de Notificação de Pedidos via Webhook (não implementado) - 2026-08-31 - 5 ADRs de alta prioridade, 0 de média prioridade
- **ORDERS**: Pedidos (núcleo do domínio, ponto de integração direto com WEBHOOKS) - 2026-08-31 - 1 ADR de alta prioridade, 0 de média prioridade
- **SHARED**: Infraestrutura Compartilhada - 2026-08-31 - 0 ADRs de alta prioridade, 0 de média prioridade (nenhuma decisão atingiu o limiar de 75 pontos — ver "Notas do Processo (SHARED)")
- **MIDDLEWARES**: Middlewares Transversais - 2026-08-31 - 0 ADRs de alta prioridade, 0 de média prioridade (nenhuma decisão atingiu o limiar de 75 pontos — ver "Notas do Processo (MIDDLEWARES)")
- **PRODUCTS**: Catálogo de Produtos e Controle de Estoque - 2026-08-31 - 0 ADRs de alta prioridade, 0 de média prioridade (nenhuma decisão atingiu o limiar de 75 pontos — ver "Notas do Processo (PRODUCTS)")
- **AUTH**: Autenticação - 2026-08-31 - 1 ADR de alta prioridade, 0 de média prioridade
- **USERS**: Gestão de Usuários - 2026-09-01 - 0 ADRs de alta prioridade, 0 de média prioridade (nenhuma decisão atingiu o limiar de 75 pontos — ver "Notas do Processo (USERS)")
- **CUSTOMERS**: Clientes - 2026-09-01 - 0 ADRs de alta prioridade, 0 de média prioridade (nenhuma decisão atingiu o limiar de 75 pontos — ver "Notas do Processo (CUSTOMERS)")
- **CONFIG**: Configuração - 2026-09-01 - 1 ADR de alta prioridade, 0 de média prioridade

### Análise Pendente
_Nenhum módulo pendente — os 9 módulos do índice de `mapping.md` foram analisados nesta Fase 2._

## ADRs de Alta Prioridade (must-document/)

### Módulo: WEBHOOKS

| Título | Categoria | Pontuação | Arquivo |
| --- | --- | --- | --- |
| Padrão Outbox no MySQL para Entrega de Eventos de Webhook | Arquitetura (Infraestrutura / Consistência de Dados) | 145/150 | [Link](./potential-adrs/must-document/WEBHOOKS/outbox-pattern-para-entrega-de-eventos-de-webhook.md) |
| Worker de Entrega em Processo Separado com Polling de 2 Segundos | Arquitetura (Topologia de Processos / Infraestrutura) | 130/150 | [Link](./potential-adrs/must-document/WEBHOOKS/worker-de-entrega-em-processo-separado-com-polling.md) |
| Autenticação de Webhooks via HMAC-SHA256 com Secret por Endpoint e Rotação | Segurança (Autenticação de Integração Externa) | 130/150 | [Link](./potential-adrs/must-document/WEBHOOKS/autenticacao-hmac-sha256-com-secret-por-endpoint.md) |
| Garantia de Entrega At-Least-Once com Idempotência via X-Event-Id | Arquitetura (Protocolo / Contrato de API Externa) | 125/150 | [Link](./potential-adrs/must-document/WEBHOOKS/garantia-de-entrega-at-least-once-com-idempotencia-via-x-event-id.md) |
| Política de Retry com Backoff Exponencial e Dead Letter Queue | Arquitetura (Resiliência / Confiabilidade) | 115/150 | [Link](./potential-adrs/must-document/WEBHOOKS/politica-de-retry-com-backoff-exponencial-e-dead-letter-queue.md) |

### Módulo: ORDERS

| Título | Categoria | Pontuação | Arquivo |
| --- | --- | --- | --- |
| Extensão Transacional de `OrderService.changeStatus` para Publicação Atômica de Eventos (Outbox) | Arquitetura (integração/consistência transacional) | 135/150 | [Link](./potential-adrs/must-document/ORDERS/extensao-transacional-do-changestatus-para-publicacao-de-eventos-outbox.md) |

### Módulo: AUTH

| Título | Categoria | Pontuação | Arquivo |
| --- | --- | --- | --- |
| Estratégia de Autenticação JWT Stateless com RBAC Embutido e Hash de Senha via bcrypt | Segurança (Autenticação e Autorização) | 140/150 | [Link](./potential-adrs/must-document/AUTH/estrategia-de-autenticacao-jwt-stateless-com-bcrypt.md) |

### Módulo: CONFIG

| Título | Categoria | Pontuação | Arquivo |
| --- | --- | --- | --- |
| Prisma como ORM e Camada Única de Acesso a Dados | Arquitetura (ORM / Camada de Acesso a Dados) | 140/150 | [Link](./potential-adrs/must-document/CONFIG/prisma-como-orm-e-camada-de-acesso-a-dados.md) |

## ADRs de Média Prioridade (consider/)

Nenhuma para os módulos WEBHOOKS, ORDERS, SHARED, MIDDLEWARES, PRODUCTS, AUTH, USERS, CUSTOMERS ou CONFIG nesta análise.

## Resumo

- Alta Prioridade: 8 ADRs (5 WEBHOOKS + 1 ORDERS + 1 AUTH + 1 CONFIG)
- Média Prioridade: 0 ADRs
- Total: 8 ADRs
- Módulos Analisados: 9 de 9 (WEBHOOKS, ORDERS, SHARED, MIDDLEWARES, PRODUCTS, AUTH, USERS, CUSTOMERS, CONFIG) — análise da Fase 2 concluída para todos os módulos identificados em `mapping.md`

## Notas do Processo (WEBHOOKS)

- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente (nenhuma ADR formal existe ainda para comparação).
- Histórico de Git não aplicável para nenhuma das 5 decisões acima: os arquivos/tabelas relevantes (`webhook_outbox`, `webhook_dead_letter`, `src/worker.ts`, lógica HMAC, lógica de `event_id`) ainda não existem no código. `git log` do repositório mostra um único commit (`init repository`), sem granularidade temporal.
- Toda evidência de decisão para o módulo WEBHOOKS vem de `TRANSCRICAO.md` (reunião técnica de ~55min), cruzada com o único ponto de integração real já existente no código: `src/modules/orders/order.service.ts` (método `changeStatus`).
- Decisão avaliada e **descartada** como ADR separada: "Reuso de padrões existentes do projeto" (estrutura de módulo controller/service/repository/routes/schemas, `AppError`/`WEBHOOK_*`, Pino, error middleware centralizado — confirmada em `[09:27]-[09:30]`). Pontuação estimada 55/150 (abaixo do limiar de 75): não há alternativa debatida (não se cogitou microserviço separado ou outro padrão de erro), o que enfraquece o critério "Evidente"; é essencialmente a aplicação de convenções já estabelecidas em outros módulos (SHARED/MIDDLEWARES), não uma decisão nova. Recomenda-se mencioná-la apenas como contexto/consequência dentro das ADRs de Outbox e Worker, não como ADR própria.
- Decisões classificadas como **não arquiteturais pela própria equipe na reunião** e consolidadas dentro das ADRs relacionadas (não geraram arquivo próprio): TLS obrigatório (`[09:23]`, dentro da ADR de HMAC), limite de payload 64KB (`[09:23]-[09:24]`, dentro da ADR de HMAC), formato exato de payload e headers (`[09:43]-[09:45]`, dentro da ADR de At-Least-Once/X-Event-Id), IDs em UUID para a outbox (`[09:50]-[09:51]`, dentro da ADR de Outbox), timeout de 10s do worker (`[09:42]`, dentro da ADR de Retry/DLQ).
- Decisões/funcionalidades de produto, não arquiteturais, **descartadas** integralmente (Sinal de Alerta 2 — fluxo/regra de negócio): CRUD de configuração de webhook aberto a qualquer role autenticada "por enquanto" (`[09:36]-[09:37]`, falha o critério de Estabilidade — a própria equipe já sinaliza que pode mudar); endpoint `GET /webhooks/:id/deliveries` de histórico de entregas (`[09:34]`, requisito funcional).
- Questões marcadas como **inconclusivas/adiadas** na transcrição, registradas aqui para uso no RFC como "Questões em Aberto", não como ADR: rate limiting de envio (`[09:38]-[09:39]`, "observar e decidir depois"); garantia de ordering entre múltiplos workers (`[09:12]-[09:13]`, solução futura cogitada mas não especificada); notificação por e-mail em falhas repetidas (`[09:37]-[09:38]`, adiada para fase futura); dashboard visual para clientes (`[09:39]-[09:40]`, rejeitada para este escopo, fica com o time de frontend).
- Nenhuma discrepância entre transcrição e código foi encontrada: a reunião é anterior à implementação, então a ausência total de código de webhooks é a lacuna esperada, não uma divergência.
- **Nota de coordenação entre análises paralelas**: esta análise do módulo WEBHOOKS foi executada em paralelo a uma análise independente do módulo ORDERS (ver seção "Notas do Processo (ORDERS)" abaixo). Durante a execução, escritas concorrentes neste `potential-adrs-index.md` e nos arquivos individuais causaram perda temporária de 4 dos 5 arquivos de ADR potenciais de WEBHOOKS, que foram recriados nesta rodada. A análise de ORDERS chegou a registrar uma nota classificando um dos arquivos de WEBHOOKS remanescentes como "órfão"/fora de escopo — essa nota está desatualizada: o módulo WEBHOOKS foi formalmente analisado nesta mesma execução, com as 5 decisões-núcleo cobertas (outbox, worker, retry/DLQ, HMAC, at-least-once).

## Notas do Processo (ORDERS)

- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente (nenhuma ADR formal existe ainda para comparação).
- Histórico de Git não informativo para a decisão acima: o repositório tem um único commit de inicialização (`7ef4317 init repository`, 2026-06-24); `order.service.ts` não tem evolução registrada além desse commit.
- Toda evidência de decisão de negócio vem de `TRANSCRICAO.md` (reunião técnica de ~55min), cruzada com o único ponto de integração real já existente no código: `src/modules/orders/order.service.ts` (método `changeStatus`, linhas 126-179), dentro do `prisma.$transaction` que hoje debita/repõe estoque, atualiza `Order.status` e grava `OrderStatusHistory`.
- **Decisões confirmadas na reunião mas descartadas como ADR de ORDERS** (pertencem estruturalmente ao módulo WEBHOOKS, agora coberto pelas 5 ADRs potenciais listadas acima): outbox pattern no MySQL (tabela `webhook_outbox`), worker de polling separado (2s), política de retry com backoff + DLQ, autenticação HMAC-SHA256 com secret por endpoint, entrega at-least-once via `X-Event-Id`. Mencionadas na ADR potencial de ORDERS apenas como observação de acoplamento via o ponto real de integração (`publishWebhookEvent(tx, ...)` chamado dentro do `tx` de `changeStatus`).
- **Descartado por Sinal de Alerta 2 (fluxo/regra de negócio)**: a máquina de estados de pedidos (`src/modules/orders/order.status.ts`) não gerou ADR própria — é modelagem de domínio/regra de negócio (transições `PENDING → PAID → PROCESSING → SHIPPED → DELIVERED`), não um padrão ou estilo arquitetural.
- **Descartado por Sinal de Alerta 5 (granularidade excessiva)**: a função `publishWebhookEvent(tx, order, fromStatus, toStatus)` isolada não gerou ADR própria — é um detalhe de implementação da decisão maior de outbox transacional, consolidado dentro da ADR de ORDERS já criada.
- **Descartado (abaixo do limiar de pontuação)**: "reuso de padrões existentes do projeto" (estrutura controller/service/repository/routes/schemas, `AppError`/`WEBHOOK_*`, Pino, error middleware centralizado) — confirmada na reunião (`[09:27]-[09:30]`), mas sem alternativa debatida e sem ser uma decisão nova (aplicação de convenção já estabelecida), pontuação estimada abaixo de 75.
- **Incerteza sinalizada**: o subfluxo de como `publishWebhookEvent` consultaria a configuração de webhook por cliente (filtrar inserção quando não há interessados no status, `[09:33]-[09:34]`) dentro da mesma `tx` não foi detalhado com profundidade suficiente na reunião — registrado como questão em aberto na ADR potencial, não como decisão fechada.
- Nenhuma discrepância entre transcrição e código foi encontrada para ORDERS: a reunião é anterior à implementação do webhook, então a ausência de código de outbox/webhooks em `order.service.ts` é a lacuna esperada, não uma divergência.

## Notas do Processo (SHARED)

- **Resultado**: nenhuma decisão do módulo SHARED atingiu o limiar mínimo de 75 pontos. Nenhum arquivo de ADR potencial foi criado em `potential-adrs/must-document/SHARED/` nem em `potential-adrs/consider/SHARED/`. Este é um resultado válido e esperado do processo (critério de seletividade extrema), não uma omissão.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --format='%ai|%s' -- src/shared/` retorna apenas o commit único `7ef4317 init repository` (2026-06-24); não há granularidade temporal (introdução, evolução, commits recentes) para nenhum dos arquivos analisados.
- Componentes do módulo SHARED analisados: `src/shared/errors/app-error.ts` + `http-errors.ts` (hierarquia `AppError`), `src/shared/http/response.ts` (paginação padrão), `src/shared/logger/index.ts` (Pino com redaction).
- Nenhum componente do SHARED se enquadra nas categorias universais da Etapa 0 (Infraestrutura externa, Framework principal, ORM, Protocolo de API) nem em infraestrutura crítica de domínio — Pino roda in-process (não é serviço externo provisionado) e não há evidência na transcrição de que logging/observabilidade seja um requisito crítico do produto. Todas as decisões do módulo, portanto, passam pelo funil completo de Sinais de Alerta + Pontuação a partir de base 0.
- **Hierarquia `AppError`/`errorCode` (padrão de erro tipado)** — avaliada com evidência cruzada Código + Transcrição:
  - Código: usada hoje pelas 5 subclasses em `http-errors.ts` e consumida por todos os módulos existentes (`AUTH`, `USERS`, `CUSTOMERS`, `PRODUCTS`, `ORDERS`) e pelos 3 middlewares (`error.middleware.ts`, `validate.middleware.ts`, `auth.middleware.ts`).
  - Transcrição: `[09:28]-[09:30]` Bruno/Larissa — a equipe decide **reaproveitar** esse padrão para o módulo WEBHOOKS (prefixo `WEBHOOK_*`), sem debater nenhuma alternativa de modelo de erro (RFC 7807, exceptions não tipadas, etc.).
  - Passa nos 3 E's (Estrutural: sim, cruza toda a API; Estável: sim, desde a inicialização) mas falha parcialmente em "Evidente" — não há alternativa debatida em nenhuma fonte, é convenção pré-existente sendo apenas estendida.
  - Pontuação estimada: Escopo+Impacto 20 (mais de 5 módulos, mas não toca a chamada HTTP de saída dos webhooks, só a API de configuração) + Custo de Mudança 20 (errorCode é contrato público de API; mudar exigiria coordenação de clientes externos, 2-6 meses) + Conhecimento da Equipe 25 (todo engenheiro precisa usar corretamente) = **65/150** → abaixo do limiar de 75. **Descartada.**
  - Esta pontuação é consistente com a nota já registrada em "Notas do Processo (WEBHOOKS)", que avaliou a mesma decisão sob a ótica de "reuso de padrões existentes" e chegou a 55/150 pelo mesmo motivo (convenção já estabelecida, sem alternativa debatida). Não se trata de decisão duplicada nem de nova exploração — é o mesmo achado revisitado a partir do módulo de origem (SHARED), com pontuação levemente distinta pela mudança de ótica, mas o mesmo veredito: abaixo do limiar.
- **Formato de resposta paginada (`shared/http/response.ts`)** — descartada por Sinal de Alerta 4 (implementação trivial): 1 arquivo, função pura de poucas linhas, sem menção em `TRANSCRICAO.md`, sem alternativa cogitada, usada apenas em 3 módulos de listagem (`customers`, `products`, `orders`). Falha o critério "Evidente" (nada a debater) e "Estrutural" tem alcance limitado. Pontuação estimada muito abaixo de 75 (não formalizada em detalhe por ser claramente trivial).
- **Configuração do logger Pino com redaction de campos sensíveis (`shared/logger/index.ts`)** — descartada por falta de evidência cruzada suficiente: a transcrição **não discute** estratégia de logging/observabilidade em nenhum momento (nem para a API existente, nem para o worker de webhooks — o próprio `mapping.md` já sinaliza essa lacuna na seção "Preocupações Transversais": "A reunião não decide explicitamente uma estratégia de observabilidade específica para o worker... não confirmado em TRANSCRICAO.md"). Sem uma decisão classificável como confirmada/proposta/rejeitada na transcrição, e sem se enquadrar em nenhuma categoria da Etapa 0, a pontuação fica baixa em todas as dimensões (estimada em torno de 50/150) e não seria uma decisão *evidente* o suficiente para justificar uma ADR isolada neste momento. Sinalizada aqui como incerteza/lacuna, não como decisão descartada por rejeição — pode voltar a ser avaliada se o FDD/RFC vier a tratar explicitamente de observabilidade do worker.
- Nenhuma discrepância entre transcrição e código foi encontrada para SHARED: os componentes analisados são infraestrutura pré-existente e estável, consistente com o que a reunião descreve como "padrões já existentes" a serem reaproveitados.

## Notas do Processo (MIDDLEWARES)

- **Resultado**: nenhuma decisão do módulo MIDDLEWARES atingiu o limiar mínimo de 75 pontos. Nenhum arquivo de ADR potencial foi criado em `potential-adrs/must-document/MIDDLEWARES/` nem em `potential-adrs/consider/MIDDLEWARES/`. Este é um resultado válido e esperado do processo (critério de seletividade extrema), não uma omissão.
- **Nota de recuperação de índice**: esta entrada recria a análise do módulo MIDDLEWARES, que havia sido executada anteriormente em paralelo com WEBHOOKS/ORDERS/SHARED, mas cuja seção neste `potential-adrs-index.md` foi perdida por uma condição de corrida em escritas concorrentes do mesmo arquivo (o módulo reapareceu como "Análise Pendente"). A análise foi refeita do zero nesta execução (sem paralelismo), preservando integralmente as seções já existentes de WEBHOOKS, ORDERS e SHARED.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --format='%ai|%s' -- src/middlewares/` retorna apenas o commit único `7ef4317 init repository` (2026-06-24); não há granularidade temporal para nenhum dos 4 arquivos do módulo.
- Componentes do módulo MIDDLEWARES analisados: `src/middlewares/auth.middleware.ts` (`authenticate` + `requireRole`), `src/middlewares/validate.middleware.ts` (aplica schemas Zod, converte `ZodError` em `ValidationError`), `src/middlewares/request-logger.middleware.ts` (`X-Request-Id`, log estruturado `http_request` via Pino), `src/middlewares/error.middleware.ts` (serialização central de `AppError`/`ZodError`/erros Prisma conhecidos em JSON).
- **Distinção de escopo importante**: o mecanismo de autenticação em si (JWT, emissão/verificação de token) é decisão do módulo **AUTH** (ainda não analisado), não de MIDDLEWARES. O escopo de MIDDLEWARES, conforme `mapping.md` (linha 99), é estritamente a *camada de enforcement* (o middleware Express que verifica o token e aplica RBAC), não a escolha do mecanismo de autenticação.
- Nenhum componente do MIDDLEWARES se enquadra nas categorias universais da Etapa 0 (Infraestrutura externa, Framework principal, ORM, Protocolo de API) nem em infraestrutura crítica de domínio isolada — os 4 middlewares são implementações de suporte (enforcement, validação, logging, formatação de erro), não decisões de plataforma. O Framework HTTP (Express 4.21, `docs/adrs/mapping.md` linha 9) foi identificado como possível candidato à Categoria 2 da Etapa 0, mas: (a) é uma escolha de projeto inteiro, anterior à reunião, sem qualquer menção em `TRANSCRICAO.md` (a reunião trata exclusivamente da feature de webhooks, não da escolha do framework HTTP da API já existente); (b) não está estruturalmente escopado a MIDDLEWARES no `mapping.md` (aparece em CONFIG/app.ts e em todos os módulos de rotas). Sinalizado aqui como fora do escopo desta análise, não avaliado como ADR de MIDDLEWARES.
- Todas as decisões do módulo, portanto, passam pelo funil completo de Sinais de Alerta + Pontuação a partir de base 0.
- **`authenticate` + `requireRole` (enforcement de JWT/RBAC no Express)** — avaliado com evidência cruzada Código + Transcrição:
  - Código: usado hoje por todos os módulos protegidos (`AUTH` emite o token; `USERS`, `CUSTOMERS`, `PRODUCTS`, `ORDERS` usam `authenticate`/`requireRole` em `*.routes.ts`).
  - Transcrição: `[09:35]-[09:36]` Larissa — "role ADMIN obrigatório no replay e a gente reaproveita o requireRole que já existe" — decisão **confirmada**, mas explicitamente de **reuso**, sem nenhuma alternativa de autorização debatida (não se cogitou policy engine, ABAC, escopos de OAuth, etc.).
  - Falha o critério "Evidente" dos 3 E's (nada a debater, é extensão de mecanismo pré-existente) e falha "Estrutural" no recorte específico de MIDDLEWARES (a decisão estrutural de RBAC pertence ao módulo AUTH; aqui é só a aplicação pontual do middleware a um novo endpoint).
  - Sinal de Alerta 5 (granularidade excessiva): esta decisão é um detalhe de implementação que já está consolidado dentro da ADR potencial "Política de Retry com Backoff Exponencial e Dead Letter Queue" (`potential-adrs/must-document/WEBHOOKS/`), que cobre o endpoint de replay de DLQ.
  - Pontuação estimada: Escopo+Impacto 15 (3-4 módulos tocados, mas sem mudança estrutural) + Custo de Mudança 10 (trocar `requireRole` por outro mecanismo é 1-2 semanas, é só um middleware) + Conhecimento da Equipe 15 (relevante para quem mexe em rotas protegidas) = **40/150** → muito abaixo do limiar de 75. **Descartada.**
- **`error.middleware.ts` (serialização central de erros)** — avaliado com evidência cruzada Código + Transcrição:
  - Transcrição: `[09:29]` Bruno — "O middleware de erro centralizado já trata AppError, Zod e Prisma. Vai pegar nossos erros sem precisar mudar nada" — a própria equipe confirma que **zero mudança** é necessária para suportar o módulo WEBHOOKS. Não há decisão nova sendo tomada aqui, apenas a constatação de que a decisão (tomada antes da reunião, sem registro em transcrição) já resolve o caso novo.
  - Esta é essencialmente a mesma decisão já avaliada em "Notas do Processo (SHARED)" (hierarquia `AppError`/`errorCode`, 65/150) e em "Notas do Processo (WEBHOOKS)" ("reuso de padrões existentes", 55/150), revisitada aqui a partir do módulo consumidor (MIDDLEWARES). Mesmo veredito: abaixo do limiar, não se trata de achado duplicado nem de nova exploração.
  - Pontuação estimada nesta ótica: Escopo+Impacto 20 (cruza toda a API) + Custo de Mudança 15 (2-8 semanas para uma reescrita de formato de erro, ainda que improvável) + Conhecimento da Equipe 20 (relevante para qualquer erro não tratado) = **55/150** → abaixo do limiar de 75. **Descartada.**
- **`validate.middleware.ts` (validação Zod centralizada)** — descartada por ausência de evidência suficiente de decisão confirmada: a transcrição menciona apenas de passagem "padrão de schemas Zod" (`[09:30]`, dentro da lista de reuso geral de Larissa) sem debate específico sobre a abstração `validate({ body, query, params })` em si, nenhuma alternativa cogitada (ex.: validação manual, middleware por rota sem abstração comum). Classificação: discussão **inconclusiva/superficial**, não decisão confirmada específica de MIDDLEWARES. Falha "Evidente" e "Estrutural" no recorte deste módulo. Pontuação estimada muito abaixo de 75 (não formalizada em detalhe).
- **`request-logger.middleware.ts` (`X-Request-Id` + log estruturado por requisição)** — descartada por falta de evidência cruzada: **não há nenhuma menção** a este componente em `TRANSCRICAO.md` (nem ao `X-Request-Id`, nem a correlação de logs por requisição, nem à necessidade de rastrear requisições do futuro worker de webhooks). Sem decisão classificável como confirmada/proposta/rejeitada na transcrição, e sem se enquadrar em categoria da Etapa 0, a decisão não passa no critério "Evidente". Sinalizada como incerteza/lacuna (consistente com a lacuna de observabilidade já registrada em "Notas do Processo (SHARED)"), não como decisão rejeitada — pode ser revisitada se o FDD tratar explicitamente de correlação de logs entre o processo HTTP e o futuro `src/worker.ts`.
- Nenhuma discrepância entre transcrição e código foi encontrada para MIDDLEWARES: os 4 componentes são infraestrutura pré-existente e estável, e a única decisão nova mencionada na reunião sobre este módulo (reuso do `requireRole('ADMIN')` para o endpoint de replay) é consistente com o padrão já implementado em `auth.middleware.ts`.

## Notas do Processo (PRODUCTS)

- **Resultado**: nenhuma decisão do módulo PRODUCTS atingiu o limiar mínimo de 75 pontos. Nenhum arquivo de ADR potencial foi criado em `potential-adrs/must-document/PRODUCTS/` nem em `potential-adrs/consider/PRODUCTS/`. Este é um resultado válido e esperado do processo (critério de seletividade extrema), não uma omissão.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --format='%ai|%s' -- src/modules/products/` e `-- prisma/schema.prisma` retornam apenas o commit único `7ef4317 init repository` (2026-06-24); não há granularidade temporal (introdução, evolução, commits recentes) para nenhum dos arquivos do módulo.
- Componentes do módulo PRODUCTS analisados: `src/modules/products/product.service.ts`, `product.repository.ts`, `product.controller.ts`, `product.routes.ts`, `product.schemas.ts`, e o model `Product` em `prisma/schema.prisma` (linhas 56-72). Também foi revisado `src/modules/orders/order.service.ts` (métodos `debitStock`/`replenishStock`, linhas 204-243, e `create`, linhas 62-73) para confirmar onde a integração real entre PRODUCTS e o fluxo de pedidos acontece.
- **Achado estrutural relevante**: toda a lógica de débito/reposição de estoque (`tx.product.update` com `increment`/`decrement`) vive dentro de `OrderService` (`src/modules/orders/order.service.ts:204-243`), não em `ProductService`/`ProductRepository`. O módulo PRODUCTS em si expõe apenas CRUD simples (`list`, `getById`, `create`, `update`, `delete`) sem nenhuma lógica de negócio de estoque atômico. Isso confirma o que `mapping.md` já registrava (linha 140: "Consumido diretamente por `OrderService`") e reforça que a decisão arquitetural de manter a mutação de estoque dentro da mesma transação de `changeStatus` pertence estruturalmente ao módulo ORDERS, já coberta pela ADR potencial "Extensão Transacional de `OrderService.changeStatus` para Publicação Atômica de Eventos (Outbox)" (`potential-adrs/must-document/ORDERS/`). Não há decisão nova ou distinta a documentar do lado de PRODUCTS — seria duplicata.
- Nenhum componente do PRODUCTS se enquadra nas categorias universais da Etapa 0 (Infraestrutura externa, Framework principal, ORM, Protocolo de API) nem em infraestrutura crítica de domínio — o módulo não introduz nenhuma tecnologia nova: usa o mesmo Prisma/Zod/Express de todos os outros módulos. A escolha do Prisma como ORM é uma decisão de projeto inteiro, estruturalmente ligada ao singleton `PrismaClient` de `src/config/database.ts` (módulo CONFIG, ainda não analisado nesta Fase 2) e não específica de PRODUCTS — mesma lógica de exclusão já aplicada ao framework Express na análise de MIDDLEWARES (ver nota acima). Sinalizado como fora do escopo desta análise; se vier a ser avaliada, o local mais natural é CONFIG.
- Todas as decisões do módulo, portanto, passam pelo funil completo de Sinais de Alerta + Pontuação a partir de base 0.
- **Preço em centavos (`priceCents: Int`, nunca float)** — avaliado com evidência cruzada Código + Transcrição:
  - Código: campo `Int` em `prisma/schema.prisma:61`, usado consistentemente em `product.schemas.ts` (`priceCents: z.number().int().nonnegative()`) e propagado para `unitPriceCents`/`totalCents`/`subtotalCents` em `order.service.ts`.
  - Transcrição: **não há nenhuma discussão** sobre a representação de preço na entidade `Product`. A única menção a "cents" na reunião é `total_cents` como campo do **payload do evento de webhook** (`[09:43]` Diego), que é uma decisão do módulo WEBHOOKS (já consolidada na ADR potencial "Garantia de Entrega At-Least-Once com Idempotência via X-Event-Id"), não uma decisão sobre o modelo de dados de `Product`.
  - Falha Sinal de Alerta 1 (modelagem de domínio — é um tipo de campo de entidade, não um estilo de modelagem debatido) e falha o critério "Evidente" (nenhuma alternativa como `Decimal`/float foi cogitada em nenhuma fonte). Pontuação estimada muito abaixo de 75. **Descartada.**
- **CRUD padrão de produto (controller/service/repository/routes/schemas)** — mesma estrutura modular de todos os outros módulos do projeto, sem nenhuma menção específica a PRODUCTS em `TRANSCRICAO.md` (a reunião cita reuso de padrões apenas no contexto genérico de construir o módulo WEBHOOKS, `[09:27]-[09:30]`). Mesmo achado já avaliado e descartado sob a ótica de WEBHOOKS (55/150) e SHARED (65/150) — revisitado aqui a partir do módulo consumidor, mesmo veredito: abaixo do limiar. **Descartada.**
- **Regra de unicidade de SKU (`ConflictError('SKU_ALREADY_USED')`)** — descartada por Sinal de Alerta 2/4 (regra de negócio trivial, localizada em `product.service.ts:30-34` e `45-52`, sem qualquer menção em `TRANSCRICAO.md`, sem alternativa debatida).
- **Flag `active` bloqueando produtos inativos em novos pedidos** (`order.service.ts:66-73`, `UnprocessableEntityError('INACTIVE_PRODUCT')`) — descartada por Sinal de Alerta 2 (fluxo/regra de negócio). Não há menção em `TRANSCRICAO.md`; a validação é aplicada dentro de `OrderService.create`, não em PRODUCTS.
- **Observação de risco registrada, mas não elevada a ADR** (sem evidência de decisão confirmada na transcrição, portanto não classificável): o endpoint `PATCH /products/:id` permite alterar `stockQuantity` diretamente (`product.schemas.ts:14`, `updateProductSchema = createProductSchema.partial()`), sem passar pelas validações atômicas de `debitStock`/`replenishStock` de `OrderService`. Isso não foi discutido na reunião (que trata exclusivamente da feature de webhooks) e não há evidência de que seja uma decisão arquitetural deliberada ou um problema priorizado pela equipe — apenas uma característica do CRUD genérico existente. Registrado aqui como possível ponto de atenção para o FDD/RFC, não como ADR.
- Nenhuma discrepância entre transcrição e código foi encontrada para PRODUCTS: o módulo é infraestrutura de catálogo pré-existente e estável; a reunião não trata da entidade `Product` além de excluir `items`/dados de produto do payload de webhook (decisão já consolidada no módulo WEBHOOKS).

## Notas do Processo (AUTH)

- **Resultado**: 1 ADR potencial de alta prioridade criada: "Estratégia de Autenticação JWT Stateless com RBAC Embutido e Hash de Senha via bcrypt" (140/150), elevada à Etapa 0 pela categoria de infraestrutura crítica de domínio "Autenticação" (sistema voltado a usuários), com evidência de decisão estabelecida e estável no código.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --format='%ai|%s' -- src/modules/auth/auth.service.ts`, `-- src/middlewares/auth.middleware.ts` e `-- src/config/env.ts` retornam apenas o commit único `7ef4317 init repository` (2026-06-24 15:41:54 -0300); `git log --all --grep="auth\|jwt\|token\|role"` não retorna nenhum commit adicional. Não há granularidade temporal (introdução incremental, evolução, commits recentes) para nenhum dos arquivos do módulo — consistente com o padrão já registrado nas demais análises desta Fase 2.
- Componentes do módulo AUTH analisados: `src/modules/auth/auth.service.ts` (`login`, `register`, `signToken`), `src/modules/auth/auth.controller.ts`, `src/modules/auth/auth.routes.ts` (`POST /register`, `POST /login`, `GET /me`), `src/modules/auth/auth.schemas.ts` (`registerSchema`/`loginSchema`, enum de role). Também revisados `src/middlewares/auth.middleware.ts` (`authenticate`/`requireRole`, já parcialmente coberto na análise de MIDDLEWARES sob a ótica de *enforcement*, aqui sob a ótica do *mecanismo* em si) e `src/config/env.ts` (validação de `JWT_SECRET`/`JWT_EXPIRES_IN`).
- **Lacuna explícita da transcrição**: `TRANSCRICAO.md` **não discute diretamente** a escolha do JWT como mecanismo de autenticação de usuários do OMS, bcrypt para hash de senha, ou o modelo RBAC de 2 papéis (`ADMIN`/`OPERATOR`). A reunião trata exclusivamente da feature de webhooks. As únicas menções a JWT/token/role na busca por palavras-chave (`grep -ni "jwt\|token\|login\|auth\|senha\|bcrypt\|role\|admin\|operator\|autentic" TRANSCRICAO.md`) são incidentais, tratando o mecanismo como infraestrutura já existente e não questionada: `[09:31]-[09:32]` (Marcos/Bruno/Larissa) esclarecem que o `customer_id` do cadastro de webhook não pode vir implícito do JWT (que pertence ao usuário operador interno, não ao cliente B2B) e precisa ser passado explicitamente; `[09:35]-[09:36]` (Larissa/Sofia) decidem que o endpoint de replay de DLQ exige role `ADMIN`, reaproveitando o `requireRole` "que já existe". Nenhuma dessas menções constitui uma decisão **confirmada sobre o mecanismo JWT em si** nesta reunião — é reuso de uma decisão pré-existente, não debatida. Essa distinção foi mantida com honestidade na ADR potencial criada: a pontuação/prioridade se apoia primariamente em evidência de **código** (implementação madura, usada por todos os módulos protegidos, validada por schema de env), com a categoria de infraestrutura crítica de domínio da Etapa 0 como justificativa, não uma "decisão confirmada em reunião" no sentido estrito do processo.
- **HMAC-SHA256 para webhooks — avaliado e não duplicado**: a reunião confirma HMAC-SHA256 com secret por endpoint como mecanismo de autenticação para as chamadas outbound de webhook (`[09:20]-[09:22]` Sofia), decisão já coberta pela ADR potencial `potential-adrs/must-document/WEBHOOKS/autenticacao-hmac-sha256-com-secret-por-endpoint.md`. Avaliado com julgamento se mereceria menção/consolidação aqui: **decisão tomada de tratá-la apenas como "ADR Potencial Relacionada"** dentro do arquivo de AUTH (cross-reference), não como conteúdo duplicado nem como ADR própria adicional — são mecanismos de autenticação estruturalmente distintos (JWT para usuários internos autenticados na plataforma vs. HMAC para verificação de payload por parceiros externos recebendo webhooks), servindo públicos e modelos de confiança diferentes, mas ambos compõem a "estratégia de autenticação" do sistema como um todo.
- **Descartado como ADR separada (Sinal de Alerta 5 — granularidade excessiva)**: ausência de refresh token/blacklist, expiração configurável via `JWT_EXPIRES_IN` (default `8h`), e o payload específico do token (`sub`/`email`/`role`) — todos são detalhes de implementação da decisão maior "JWT stateless com RBAC embutido", consolidados dentro da ADR potencial única de AUTH, não elevados a arquivos próprios.
- **Descartado como ADR separada (Sinal de Alerta 3 — detalhe de configuração)**: validação de `JWT_SECRET` com mínimo de 16 caracteres em `env.ts` — é um valor de validação isolado, sem debate em nenhuma fonte, consolidado como evidência de suporte dentro da ADR principal, não como decisão isolada.
- Nenhuma discrepância entre transcrição e código foi encontrada para AUTH: o mecanismo de código é estável e a reunião apenas o reutiliza sem alterá-lo ou questioná-lo.

## Notas do Processo (USERS)

- **Resultado**: nenhuma decisão do módulo USERS atingiu o limiar mínimo de 75 pontos. Nenhum arquivo de ADR potencial foi criado em `potential-adrs/must-document/USERS/` nem em `potential-adrs/consider/USERS/`. Este é um resultado válido e esperado do processo (critério de seletividade extrema), não uma omissão — consistente com o padrão já observado em SHARED, MIDDLEWARES e PRODUCTS.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --format='%ai|%s' -- src/modules/users/` retorna apenas o commit único `7ef4317 init repository` (2026-06-24 15:41:54 -0300); `git log --all --grep="user\|role\|toPublic\|password"` não retorna nenhum commit adicional. Não há granularidade temporal para nenhum dos 5 arquivos do módulo.
- Componentes do módulo USERS analisados (todos os 5 arquivos): `src/modules/users/user.controller.ts` (apenas `GET /:id`, sem endpoints de criação/edição/listagem/remoção diretos no próprio módulo), `user.repository.ts` (`findById`, `findByEmail`, `create`), `user.routes.ts` (`GET /:id`, `authenticate` + `requireRole('ADMIN')`), `user.schemas.ts` (`createUserSchema`), `user.service.ts` (`createUser`, `getById`, `static toPublic()`). Também revisados `src/modules/auth/auth.service.ts` (confirma que `AuthService.register` delega inteiramente para `UserService.createUser`, isto é, a criação de usuário é acionada pelo fluxo de auto-registro público de AUTH, não por um endpoint administrativo em USERS) e `src/middlewares/auth.middleware.ts` (tipo `AuthUser`) e `prisma/schema.prisma` (`enum UserRole`, model `User`, relações `Order.createdBy`/`OrderStatusHistory.changedBy`).
- **Lacuna explícita da transcrição**: `TRANSCRICAO.md` **não discute em nenhum momento** a gestão de usuários internos (CRUD de `User`, papéis `ADMIN`/`OPERATOR` como conceito de domínio, ou o padrão `toPublic()`). Busca por palavras-chave (`grep -ni "usuário\|user\|operador\|admin\|role\|passwordhash\|topublic" TRANSCRICAO.md`) só retorna menções incidentais já registradas nas Notas do Processo (AUTH): `[09:31]-[09:32]` (o `customer_id` do webhook não pode vir implícito do JWT do "usuário operador") e `[09:35]-[09:36]` (replay de DLQ exige role `ADMIN`, reaproveitando `requireRole`). Nenhuma delas constitui decisão confirmada sobre o módulo USERS em si — é reuso de infraestrutura de usuários/roles pré-existente, tratada como dado de contexto, não debatida. Essa ausência é sinalizada aqui com honestidade, sem inventar menções que não existem.
- Nenhum componente de USERS se enquadra nas categorias universais da Etapa 0 (Infraestrutura externa, Framework principal, ORM, Protocolo de API) nem em infraestrutura crítica de domínio (não é gateway de pagamento, não é provedor de autenticação/SSO — o mecanismo de autenticação em si já foi avaliado e coberto pela ADR potencial de AUTH). USERS é essencialmente CRUD interno reaproveitando Prisma/Zod/bcrypt já cobertos em outros módulos. Todas as decisões, portanto, passam pelo funil completo de Sinais de Alerta + Pontuação a partir de base 0.
- **Padrão `toPublic()` para nunca vazar `passwordHash`** (`user.service.ts:42-51`) — avaliado com evidência cruzada Código + Transcrição:
  - Código: método estático que projeta `User` (com `passwordHash`) em `PublicUser` (sem `passwordHash`), usado em `createUser`, `getById` e também consumido por `AuthService.login` (`auth.service.ts:42`). É o único ponto de serialização de usuário em toda a API.
  - Transcrição: nenhuma menção — a reunião não discute exposição de dados sensíveis de usuário (a segurança discutida por Sofia é inteiramente sobre HMAC/secret de webhook, `[09:19]-[09:22]`, não sobre serialização de `User`).
  - Falha o critério "Evidente" dos 3 E's: é uma prática defensiva óbvia (não devolver hash de senha), sem alternativa debatida (ex.: `select` explícito no Prisma, biblioteca de serialização, DTO framework) em nenhuma fonte. Falha também "Estrutural" — é uma função pura de um arquivo, sem cruzar módulos como decisão de design.
  - Pontuação estimada: Escopo+Impacto 5 (componente único) + Custo de Mudança 5 (menos de 1 semana para alterar) + Conhecimento da Equipe 10 (ocasionalmente relevante — só quem mexe em serialização de usuário) = **20/150** → muito abaixo do limiar de 75. **Descartada.**
- **Reuso do enum `UserRole` do Prisma como suposta fonte única de verdade de papéis** — avaliado e a hipótese não se sustenta no código: `AuthUser` (`auth.middleware.ts:6-10`) e `PublicUser`/`CreateUserInput` (`user.service.ts:7-14`, `user.schemas.ts:11`) declaram o tipo do papel como união literal `'ADMIN' | 'OPERATOR'` escrita à mão em cada arquivo, **não** importando o enum `UserRole` de `@prisma/client`. Ou seja, ao contrário do que o resumo em `mapping.md` (linha 121) sugere ("enum UserRole... reaproveitado no AuthUser"), há duplicação de literais de tipo entre `prisma/schema.prisma`, `auth.middleware.ts`, `user.service.ts` e `user.schemas.ts`, não reuso de um único tipo compartilhado. Isso não é uma decisão arquitetural deliberada (não há debate em nenhuma fonte sobre a estratégia de tipagem de roles) — é uma característica/discrepância pontual do código, registrada aqui como observação para o FDD/RFC (risco de drift se um novo papel for adicionado ao Prisma sem atualizar os literais espalhados), não como ADR.
- **Modelo RBAC de 2 papéis (`ADMIN`/`OPERATOR`)** — **não duplicado aqui**: já coberto pela ADR potencial de AUTH ("Estratégia de Autenticação JWT Stateless com RBAC Embutido e Hash de Senha via bcrypt", 140/150, `potential-adrs/must-document/AUTH/`). O RBAC é decisão estrutural de autenticação/autorização de todo o sistema, não específica de USERS; o módulo USERS apenas armazena e expõe o dado (`User.role`). Sinal de Alerta 5 (granularidade/consolidação) aplicado: não gera arquivo próprio.
- **Ausência de endpoints de CRUD completo em USERS** (só existe `GET /:id`; criação ocorre via `POST /auth/register`, sem `PATCH`/`DELETE`/`GET /` de listagem) — descartada por Sinal de Alerta 2 (característica de fluxo/produto, não decisão arquitetural) e por falta total de evidência na transcrição (a reunião não trata de gestão de usuários internos). Registrada apenas como observação de possível lacuna funcional para o FDD, não como ADR.
- Nenhuma discrepância relevante entre transcrição e código foi encontrada para USERS além da já registrada acima (duplicação de literais de role em vez de reuso do enum Prisma) — a ausência total de discussão sobre USERS na reunião é a lacuna esperada (a reunião trata exclusivamente da feature de webhooks), não uma divergência de decisão.

## Notas do Processo (CUSTOMERS)

- **Resultado**: nenhuma decisão do módulo CUSTOMERS atingiu o limiar mínimo de 75 pontos. Nenhum arquivo de ADR potencial foi criado em `potential-adrs/must-document/CUSTOMERS/` nem em `potential-adrs/consider/CUSTOMERS/`. Este é um resultado válido e esperado do processo (critério de seletividade extrema), não uma omissão — consistente com o padrão já observado em SHARED, MIDDLEWARES, PRODUCTS e USERS.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --follow --format='%ai|%s' -- src/modules/customers/customer.service.ts` e `git log -10 --format='%ai|%s' -- src/modules/customers/` retornam apenas o commit único `7ef4317 init repository` (2026-06-24 15:41:54 -0300); o mesmo vale para `prisma/schema.prisma`. Não há granularidade temporal para nenhum dos 5 arquivos do módulo.
- Componentes do módulo CUSTOMERS analisados (todos os 5 arquivos): `customer.controller.ts`, `customer.repository.ts` (`list`, `findById`, `findByEmail`, `create`, `update`, `delete`), `customer.routes.ts`, `customer.schemas.ts` (`createCustomerSchema`/`updateCustomerSchema`, `addressSchema` aninhado), `customer.service.ts` (checagem de e-mail duplicado via `ConflictError('EMAIL_ALREADY_USED')`). Também revisado o model `Customer` em `prisma/schema.prisma` (linhas 40-54): campos `name`/`email` (`@unique`)/`phone`/`document`/`address: Json`, e `@@index([document])`.
- **Correção de precisão em relação ao `mapping.md`**: a linha 131 do `mapping.md` descreve "índice único em `document`", mas o schema real (`prisma/schema.prisma:52`) declara `@@index([document])` — um índice **não único** (o único campo com `@@unique`/`@unique` no model é `email`, linha 43). Registrado aqui como correção factual menor para uso no FDD/RFC; não eleva a modelagem de `document` a candidata a ADR, pois nem o código nem `TRANSCRICAO.md` debatem a escolha entre índice único vs. não único para este campo.
- Nenhum componente do CUSTOMERS se enquadra nas categorias universais da Etapa 0 (Infraestrutura externa, Framework principal, ORM, Protocolo de API) nem em infraestrutura crítica de domínio — o módulo é CRUD B2B padrão reaproveitando Prisma/Zod/Express já cobertos (e descartados como Etapa 0) em outras análises deste índice (SHARED/MIDDLEWARES/PRODUCTS/USERS). Todas as decisões, portanto, passam pelo funil completo de Sinais de Alerta + Pontuação a partir de base 0.
- **Modelagem da entidade `Customer`, incluindo `address: Json` e `@@index([document])`** — avaliada com evidência cruzada Código + Transcrição, aplicando o Sinal de Alerta 1 (Modelagem de Domínio) com o devido cuidado exigido pela tarefa:
  - Código: `address` é um campo `Json` único (`prisma/schema.prisma:46`) que recebe um objeto estruturado por Zod (`street`/`number`/`city`/`state`/`zipCode`, `customer.schemas.ts:7-13`) — não há colunas normalizadas de endereço, nem comentário no código justificando a escolha de Json em vez de uma tabela `Address` separada ou colunas individuais.
  - Transcrição: busca por palavras-chave (`grep -ni "endereço\|endereco\|address\|json\|documento" TRANSCRICAO.md`) não retorna **nenhuma** menção a `address`, "endereço" ou a formato Json em toda a reunião — a única ocorrência de "json" no arquivo é o formato do payload de evento de webhook (`[09:43]` Diego), decisão do módulo WEBHOOKS, não relacionada à modelagem de `Customer`.
  - Teste do Sinal de Alerta 1: isso descreve uma entidade de negócio (nome, e-mail, telefone, documento, endereço de um cliente B2B), não um ESTILO de modelagem debatido com trade-offs explícitos (ex.: "vamos usar Objetos de Valor imutáveis" ou "vamos usar Json flexível em vez de colunas normalizadas porque X"). Nem o código nem a transcrição fundamentam uma escolha arquitetural deliberada — **DESQUALIFICADA** por Sinal de Alerta 1.
  - Mesmo se avaliada nos 3 E's: falha "Evidente" (nenhuma alternativa — colunas normalizadas, tabela `Address` separada — foi cogitada em nenhuma fonte) e é razoavelmente "Estrutural"/"Estável" apenas de forma fraca (afeta só a camada de dados de um módulo pequeno). Pontuação estimada, se forçada: Escopo+Impacto 5 (componente único) + Custo de Mudança 10 (migrar Json para colunas normalizadas é 1-2 semanas) + Conhecimento da Equipe 10 (ocasionalmente relevante) = **25/150** → muito abaixo do limiar. **Descartada.**
- **CRUD padrão de cliente (controller/service/repository/routes/schemas)** — mesma estrutura modular de todos os outros módulos do projeto, sem nenhuma menção específica a CUSTOMERS em `TRANSCRICAO.md`. Mesmo achado já avaliado e descartado sob a ótica de WEBHOOKS (55/150), SHARED (65/150) e PRODUCTS (revisitado, mesmo veredito) — revisitado aqui a partir do módulo CUSTOMERS, mesmo veredito: abaixo do limiar. **Descartada.**
- **Regra de unicidade de e-mail (`ConflictError('EMAIL_ALREADY_USED')`)** — descartada por Sinal de Alerta 2/4 (regra de negócio trivial, localizada em `customer.service.ts:29-33` e `44-50`, sem qualquer menção em `TRANSCRICAO.md`, sem alternativa debatida).
- **Investigação central desta análise — a quem pertence a configuração de webhook (`customer_id`)**: `TRANSCRICAO.md` (`[09:21]` Bruno: "Então a tabela de configuração de webhook armazena url + secret + customer_id + estado ativo?", confirmado por Sofia) indica que a configuração de webhook será uma **tabela própria e nova** que referencia `customer_id`, não um campo adicionado ao model `Customer` existente. Em `[09:31]-[09:32]` (Marcos/Bruno/Larissa), a equipe **confirma** que o `customer_id` do cadastro de webhook é passado explicitamente no body/path do endpoint (não vem implícito do JWT, que pertence ao usuário operador interno, não ao cliente B2B) — esta é uma **decisão confirmada**, mas sobre o *endpoint de cadastro de webhook* (módulo WEBHOOKS), não sobre o *model* `Customer` em si.
  - Classificação honesta, conforme exigido pela tarefa: em nenhum momento da reunião a equipe declara explicitamente que o model `Customer` **não será alterado**, nem debate alternativas de modelagem (ex.: relação Prisma formal `Customer.webhookConfig` vs. tabela desacoplada apenas com `customer_id` como string solta, sem FK declarada no Prisma). O que existe no código hoje é apenas: `Customer` não tem nenhuma referência a webhooks, e a única relação declarada é `orders Order[]` (`prisma/schema.prisma:50`).
  - **Classificação**: DISCUSSÃO INCONCLUSIVA quanto à modelagem formal da relação `Customer` ↔ configuração de webhook (existência ou não de uma relação Prisma explícita, comportamento de cascade em caso de exclusão de cliente, etc.). A afirmação do `mapping.md` (linha 130) de que "a reunião não decide se webhook pertence a Customer ou a um novo conceito" está correta e é reafirmada aqui após leitura completa da transcrição — não foi encontrada nenhuma decisão adicional que feche essa lacuna.
  - **Não é ADR do módulo CUSTOMERS**: não há decisão confirmada que altere a entidade `Customer`; o que existe é a decisão confirmada (já pertencente ao módulo WEBHOOKS) de que a tabela de configuração de webhook é separada e referencia `customer_id`. Não deve ser tratada como ADR de CUSTOMERS. Se a equipe responsável pelas ADRs formais do módulo WEBHOOKS ainda não tiver registrado essa distinção separada/relacionada, recomenda-se conferir se algum dos 5 ADRs potenciais de WEBHOOKS já cobre esse ponto (a ADR de Outbox, por exemplo, é a mais próxima estruturalmente).
  - **Recomendação para o RFC**: registrar como "Questão em Aberto" — se `customer_id` na tabela de webhook será uma foreign key formal para `customers.id` (com `ON DELETE` definido) ou apenas um campo desacoplado, e se o Prisma schema de `Customer` ganhará uma relação inversa (`webhookConfigs WebhookConfig[]`) análoga a `orders Order[]`. Nenhuma fonte (código ou transcrição) resolve essa questão hoje.
- Nenhuma discrepância entre transcrição e código foi encontrada para CUSTOMERS além da correção factual do `mapping.md` já registrada acima (índice não-único em vez de único) — a ausência de qualquer campo/relação de webhook no model `Customer` é consistente com o fato de a reunião ser anterior à implementação e com a lacuna já sinalizada no `mapping.md`.

## Notas do Processo (CONFIG)

- **Resultado**: 1 ADR potencial de alta prioridade criada: "Prisma como ORM e Camada Única de Acesso a Dados" (140/150), elevada à Etapa 0 pela Categoria 3 (ORM/Camada de Acesso a Dados) — categoria universal que garante pontuação base de 75 e piso mínimo de 100 (`must-document/`) segundo a Regra Especial para Categorias Universais, independentemente de a escolha do Prisma ter sido debatida nesta reunião especificamente. Este é o encaminhamento que as análises de **MIDDLEWARES** (linha 102) e **PRODUCTS** (linha 125) já haviam sinalizado como pendente para este módulo.
- `docs/adrs/generated/` não existe no repositório nesta análise — checagem de duplicatas/relações com ADRs formais já escritas foi pulada graciosamente.
- Histórico de Git não informativo: `git log --follow --format='%ai|%s' -- src/config/` retorna apenas o commit único `7ef4317 init repository` (2026-06-24 15:41:54 -0300), consistente com o padrão já registrado em todas as demais análises desta Fase 2.
- Componentes do módulo CONFIG analisados (os 2 arquivos): `src/config/database.ts` (`createPrismaClient()`, singleton `prisma` exportado) e `src/config/env.ts` (schema Zod de env vars, `process.exit(1)` em config inválida via `loadEnv()`). Também revisado `src/app.ts` (`buildControllers(prisma)`, injeção manual do singleton em cada `*Repository`/`*Service` de módulo, sem container de DI).
- **Prisma ORM (Categoria 3, Etapa 0)** — avaliação honesta da evidência de transcrição: `TRANSCRICAO.md` **não debate a escolha do Prisma** como ORM em nenhum momento (a reunião trata exclusivamente da feature de webhooks e assume o Prisma como infraestrutura já dada). A única decisão **confirmada** relacionada a CONFIG na transcrição é em `[09:29]-[09:30]` (Diego pergunta, Bruno responde, Larissa fecha): o worker de webhooks, por ser um processo Node separado, precisa de sua **própria instância** de `PrismaClient` (mesma `DATABASE_URL`, instância nova), aplicando diretamente o padrão de singleton-por-processo já existente em `src/config/database.ts`. Essa decisão é tratada, com honestidade, como **extensão confirmada** do padrão de CONFIG à nova topologia multi-processo — não como a decisão de adotar Prisma em si (essa é anterior a qualquer histórico disponível neste repositório).
  - Pontuação: base 75 (Categoria 3) + Escopo+Impacto 20 (mais de 5 módulos — é a única via de acesso a dados de todos os módulos de negócio e, em breve, também do worker) + Custo de Mudança 25 (trocar de ORM exigiria reescrever todos os repositories, o `schema.prisma`, migrations e todo código tipado com `Prisma.TransactionClient` — inviável em curto prazo) + Conhecimento da Equipe 20 (crítico para mais de 80% das funcionalidades — todo repository e todo service que abre transação depende do singleton) = **140/150** → `must-document/`.
- **Verificação de não-duplicação com WEBHOOKS**: o arquivo `potential-adrs/must-document/WEBHOOKS/worker-de-entrega-em-processo-separado-com-polling.md` já existia e já citava `[09:29]-[09:30]` e `src/config/database.ts` como evidência de suporte para a decisão de topologia de processos, deixando explícito ali mesmo (seção "Observações Adicionais" daquele arquivo) que o reuso do `PrismaClient` é "evidência de suporte, não ADR própria". A ADR de CONFIG criada nesta análise **não duplica** aquele conteúdo: ela documenta a decisão de origem (Prisma como ORM + padrão de singleton por processo), enquanto a ADR de WEBHOOKS documenta sua consequência (o worker precisa de instância própria). Foi adicionada uma seção "Contexto de ADR Existente" na nova ADR de CONFIG fazendo essa referência cruzada explícita, e a seção "ADRs Potenciais Relacionadas" de ambos os arquivos recomenda que a ADR formal de CONFIG seja referenciada como base pela ADR formal do worker.
- **`src/config/env.ts` (validação Zod fail-fast de variáveis de ambiente, `process.exit(1)` em config inválida)** — avaliada e **descartada** como ADR própria de CONFIG:
  - Não se enquadra em nenhuma categoria universal da Etapa 0 (não é infraestrutura externa, não é framework principal, não é ORM, não é protocolo de API).
  - Zero menção em `TRANSCRICAO.md` a variáveis de ambiente, validação de configuração, `process.exit`, ou estratégia de fail-fast em toda a reunião (busca por palavras-chave confirmada sem resultado).
  - Falha parcialmente os 3 E's: é "Estável" (não muda há tempo) e minimamente "Estrutural" (todos os módulos dependem indiretamente de `env` válido), mas falha "Evidente" — é um padrão de validação Zod bastante autoexplicativo (schema + `safeParse` + `process.exit(1)`), sem alternativa cogitada em nenhuma fonte (ex.: validação manual, biblioteca `dotenv-safe`, `envalid`), e sem debate registrado.
  - Aplica-se o Sinal de Alerta 4 (implementação trivial): localizado em 1 arquivo de 27 linhas, mudança de baixo custo (<1 semana), não cruza contratos externos.
  - A única evidência de transcrição que toca `env.ts` (validação de `JWT_SECRET` com mínimo de 16 caracteres) já foi consolidada dentro da ADR potencial de AUTH como detalhe de configuração (Sinal de Alerta 3, ver "Notas do Processo (AUTH)", linha 146) — não é reavaliada aqui como decisão distinta de CONFIG.
  - Pontuação estimada, se forçada: Escopo+Impacto 10 (1-2 módulos diretamente, ainda que todos dependam indiretamente) + Custo de Mudança 5 (menos de 1 semana) + Conhecimento da Equipe 10 (ocasionalmente relevante) = **25/150** (sem a base de Etapa 0, pois não se enquadra em categoria universal) → muito abaixo do limiar de 75. **Descartada.**
- **Ausência de container de DI / injeção manual em `buildControllers`** — avaliada e **não elevada a ADR separada** (Sinal de Alerta 5 — granularidade excessiva / acoplada à decisão maior do Prisma). É a forma como o singleton de `PrismaClient` é distribuído pela árvore de objetos do projeto (padrão já usado desde a inicialização, sem alternativa — Awilix, InversifyJS, NestJS DI — cogitada em nenhuma fonte), não uma decisão de arquitetura de injeção de dependências debatida independentemente. Consolidada como evidência de suporte dentro da ADR de Prisma/CONFIG (seção "Evidência no Código"), não como arquivo próprio.
- Nenhuma discrepância entre transcrição e código foi encontrada para CONFIG: os 2 componentes são infraestrutura pré-existente e estável; a única decisão nova mencionada na reunião sobre este módulo (`PrismaClient` separado para o worker, `[09:29]-[09:30]`) é consistente com o padrão de singleton-por-processo já implementado em `database.ts` — a equipe aplica corretamente a implicação técnica desse padrão à nova topologia, sem contradizê-lo.
- **Nota de encerramento da Fase 2**: com CONFIG analisado, os 9 módulos identificados em `mapping.md` (AUTH, USERS, CUSTOMERS, PRODUCTS, ORDERS, SHARED, MIDDLEWARES, CONFIG, WEBHOOKS) foram cobertos por esta Fase 2, totalizando 8 ADRs potenciais de alta prioridade (nenhuma de média prioridade) distribuídas em 4 módulos (WEBHOOKS: 5, ORDERS: 1, AUTH: 1, CONFIG: 1); os demais 5 módulos (SHARED, MIDDLEWARES, PRODUCTS, USERS, CUSTOMERS) não geraram nenhuma ADR potencial acima do limiar de 75 pontos, um resultado válido do critério de seletividade extrema do processo.
